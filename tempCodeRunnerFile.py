ClientSocket = socket.socket()
# host = '127.0.0.1'
# port = 65431
# error = False
# try:
#     ClientSocket.connect((host, port))
# except socket.error as e:
#     Label(root, text='Cannot connect to server').grid()
#     error = True

# Response = ClientSocket.recv(1024)

# if (error == False):
#     Label(root, text='Connected to server').grid()
    
# ClientSocket.close()